# Registro de Cambios

## 2026-07-31 (fase de implementación)

- **Feature 007 · Testing y despliegue MVP** implementada: **E2E con Playwright** (`@playwright/test` + `inngest-cli` como devDeps, `npm run test:e2e`). `playwright.config.ts`: 1 worker, Chromium, `webServer` doble (Inngest Dev Server `inngest-cli dev -u http://localhost:3000/api/inngest` + `next dev`), reporter html. `e2e/storefront.spec.ts` (flujo completo: home → ficha → formulario dinámico → checkout → `/generacion/[orderId]` → `COMPLETED` por polling → preview → descargas `resultado.txt`/`resultado.png` con firma PNG → 404s) y `e2e/admin.spec.ts` (guard `/admin`→login, token ok/ko, dashboard, búsqueda/detalle con `GenerationLog`, logout); datos de prueba con `pg` directo (`e2e/helpers/db.ts` + `global-teardown.ts`) y limpieza garantizada. **8/8 en verde** con `AI_MOCK=true`. **Bug real destapado y corregido**: el pipeline guardaba la imagen vacía porque Inngest serializa a JSON el retorno de cada step y un `Buffer` se convertía en objeto (descarga `resultado.bin`, `imageBytes` 0); el smoke previo usaba `fakeRun` (identidad) y lo enmascaraba → `inngest/run-pipeline.ts` ahora devuelve `number[]` en `GENERATE_IMAGE` y reconstruye con `new Uint8Array(imageBytes)` (unit test actualizado). Smoke de integración real ampliado (texto > 40 chars, imagen > 1000 bytes) — **mock y real en verde**. **CI**: `.github/workflows/ci.yml` con jobs lint/unit/build/smoke/e2e (secrets: `DATABASE_URL`, `DIRECT_URL`, `GEMINI_MODEL`, `ADMIN_TOKEN`). Deploy preparado: `.vercelignore`, `docs/despliegue-mvp.md` (pasos + env vars + Inngest Cloud) y `docs/qa-checklist-mvp.md`; **no se ejecutó el deploy** por ausencia de credenciales Vercel (decisión del usuario; el CLI quedó listo). Notas: el cliente Prisma generado usa `import.meta` → los helpers E2E usan `pg` directo; `lib/generated/` gitignoreado y se regenera en CI con `npx prisma generate`. Lint 0 errores (4 warnings previos), 54 unit tests, build OK, BD limpia.
- **Feature 006 · Backend core** implementada: panel admin/soporte en `/admin` protegido con token (`ADMIN_TOKEN`), sin API REST (decisión de alcance del usuario). Cripto en `lib/admin/token.ts` (capa pura): `verifyAdminToken` (SHA-256 + `timingSafeEqual`), `signSession`/`verifySession` (HMAC-SHA256 en base64url, `ADMIN_SESSION_NAME`, TTL 12 h); `lib/admin/session.ts` (`getAdminSession()` con `next/headers`, `server-only`); dominio en `lib/services/admin.ts` (`getAdminDashboard`, `listOrders` con filtro por estado/búsqueda/paginación de 10, `getOrderDetail` con `generationLogs` desc). Server Actions en `features/admin/actions.ts` (`adminLoginAction` con `useActionState`, `adminLogoutAction`, `adminRetryOrderAction` que reutiliza `retryGeneration` + `sendOrderConfirmed`) — todas re-verifican la sesión (guard en `app/admin/(panel)/layout.tsx`, noindex en `app/admin/layout.tsx`). UI: `/admin/login` + `features/admin/login-form.tsx`, `features/admin/retry-order-form.tsx` y `order-table.tsx`; páginas dashboard, `/ordenes` (filtros + paginación) y `/ordenes/[orderId]` (detalle con `OrderSummary` + tabla de `GenerationLog`). Vitest 54 tests (incluye `admin-actions.test.ts` con mocks de `next/headers`/`next/navigation`), lint 0 errores, build OK, smoke `scripts/smoke-admin.test.ts` (token real, ciclo sign/verify, consultas con BD limpia; 2 archivos en el `include`). Verificado manualmente por HTTP: `/admin` sin sesión → 307 a `/admin/login`. Auth de usuarios y rate-limiting reales quedan para V1 (008/009). Decisión: `ADMIN_TOKEN` local `dev-admin-token-local` en `.env`; `.env.example` documenta la variable vacía.
- **Feature 005 · Entrega de resultado** implementada: `/generacion/[orderId]` reescrito — `QUEUED`/`PROCESSING` con polling (cliente `features/result/auto-refresh.tsx` con `router.refresh()` cada 3 s, se desmonta en estados terminales), `COMPLETED` con texto + preview `next/image` (`fill` + `object-contain` en contenedor `aspect-[4/3]`) y descarga directa nativa (`.txt` + imagen), `ERROR` con mensaje + botón "Reintentar"; `lib/services/generation.ts` agrega `retryGeneration` (solo desde `ERROR`: reset a `QUEUED`, limpia contenido/error/timestamps, `retryCount + 1`; campo ya existía → sin migración) y `getResultFile(orderId, formato)` (content-type por bytes/extensión vía `lib/ai/image-format.ts`, `found: false` si falta); Route Handler `app/api/resultado/[orderId]/route.ts` (`?formato=texto|imagen`, `&descarga=1` → `Content-Disposition`, 400/404, validación zod; consistente con `app/api` para "storage"); `features/result/actions.ts` con `retryGenerationAction` (servicio + re-dispara `order/confirmed` + `redirect`). Acceso por `orderId` (cuid) en MVP; hardening (auth + URLs firmadas) en V1 (008/009). Vitest 39 tests, lint 0 errores, build OK; smoke en ambos modos: `AI_MOCK=true` y **real** (`getResultFile` → `resultado.txt`/`resultado.png` y `resultado.jpg` con content-types correctos; retry desde COMPLETED no-op); BD limpia. Nota: TS 5.7 tipado de `Uint8Array<ArrayBufferLike>` → `new Uint8Array(bytes)` al pasar a `Response`.
- **Spike 0.2 · validación con claves reales**: pruebas end-to-end del pipeline con `AI_MOCK=false`. **Gemini**: el free tier rechaza `gemini-2.0-flash` (429 límite 0) y `gemini-2.5-*`/`gemini-1.5-flash` (404 por deprecación); el modelo que funciona es `gemini-flash-latest` → `GEMINI_MODEL` y default actualizados en `.env`, `.env.example` y `lib/ai/gemini.ts`. **Imagen (Hugging Face)**: el router rechaza `FLUX.1-schnell` para `hf-inference`/`replicate`/`nscale`/`together`; el proveedor que lo sirve es **fal-ai** con URL `router.huggingface.co/fal-ai/fal-ai/flux/schnell`, body `{ prompt }` y respuesta JSON (`images[0].url` a descargar, JPEG ~1024×768) → `lib/ai/huggingface.ts` reescrito (body `prompt`, parseo JSON + descarga, transitorios). El smoke test (`scripts/smoke-pipeline.test.ts`) ahora corre en ambos modos (mock o real según `AI_MOCK`) y se validó real: `COMPLETED` + texto Gemini + imagen JPEG (~251 KB) + 8 `GenerationLog`s; BD limpia. **Nombre de archivo de imagen corregido**: `saveTextAndImage` ya no asume `.png`; deriva la extensión de los bytes vía `lib/ai/image-format.ts` (`resultado.png` mock, `resultado.jpg` fal-ai) — smoke test con aserción según modo, unit tests con PNG y JPEG, contrato de IA v1.2.
- **Feature 004 · Pipeline de generación IA** implementada: migración `add_generation_log` (enums `PipelineStep`/`PipelineStepStatus` + `GenerationLog` con `@@index([orderId, createdAt])`); dependencias `inngest@4.14.0` y `@google/genai@2.15.0`; `lib/ai/` (contrato `AIContentProvider`/`AIImageProvider`, `interpolate.ts`, `gemini.ts` con `httpOptions.timeout`, `huggingface.ts` FLUX.1-schnell con 429/5xx→`TransientAIError`, `mock.ts`, `factory.ts` por `AI_MOCK`); `lib/services/generation.ts` (estados/logs/persistencia); `inngest/` (`client.ts`, `events.ts` con `eventType` + schema zod, `run-pipeline.ts` con `runGenerationPipeline`, `run-with-errors.ts` con `NonRetriableError`/rethrow, `pipeline.ts` con `createFunction` v4: `triggers` + `retries: 5`); `app/api/inngest/route.ts` (`serve` de `inngest/next`); `confirmOrder` → `{ order, transitioned }` y `features/checkout/actions.ts` dispara `sendOrderConfirmed` solo si transicionó; `.env.example` con `GEMINI_API_KEY`/`GEMINI_MODEL`/`HF_TOKEN`/`AI_MOCK`/`INNGEST_DEV`/`INNGEST_EVENT_KEY`/`INNGEST_SIGNING_KEY`; Vitest (34 tests) + lint + build OK; smoke test de integración (`scripts/smoke-pipeline.test.ts` con `AI_MOCK=true`, config dedicada) → `COMPLETED` + `textContent`/`imageBytes` + 8 `GenerationLog`s; datos de prueba eliminados. Notas Inngest v4: `@inngest/next` no existe (usar `serve` de `inngest/next`), `createFunction(options, handler)`, `retries` es número, el schema de `eventType` describe solo los `data`.
- **AGENTS.md**: corregidos los comandos de Prisma 7 — `migrate dev`/`deploy` no regeneran el cliente; documentado `npx prisma generate` como paso obligatorio tras migrar, `prisma studio`, y nota sobre `prisma.config.ts`/`DIRECT_URL` y el driver adapter.
- **Feature 003 · Checkout sin pasarela** implementada: migración `add_generated_result` (enum `AiResponseStatus` + modelo `GeneratedResult`); `confirmOrder` en `lib/services/orders.ts` (transición `PENDING→APPROVED` + `confirmedAt` + `upsert` de `GeneratedResult` QUEUED con snapshot del `formData`, idempotente, rechaza `REJECTED`) y `getCheckoutOrder`/`getOrderGeneration`; `features/checkout/` (`actions.ts`, `confirm-form.tsx` con `useFormStatus`, `order-summary.tsx` data-driven con labels); `/checkout/[orderId]` (resumen + botón, ya-aprobado redirige a `/generacion/[orderId]`, 404 si no existe) y stub `/generacion/[orderId]` (lee `GeneratedResult`, 404 si falta); Vitest (23 tests) + lint + build OK; smoke test real (formulario → checkout → confirmar → 303 a generacion → idempotencia con 1 solo `GeneratedResult` → 404s); pedido de prueba eliminado. Nota: `prisma migrate dev` no regenera el cliente en Prisma 7; correr `npx prisma generate` tras migrar.
- **Feature 002 · Formulario dinámico** implementada: migración `add_orders` (enum `PaymentStatus`, `Order`, `FormSubmission`); `features/forms/` con `types.ts` (FormField/FormSchema/`isFormSchema`), `build-form-schema.ts` (builder Zod data-driven: text/number/select/multiselect), `fields.tsx`, `dynamic-form.tsx` (cliente con `useActionState` + validación en cliente) y `actions.ts` (`createOrder` validando + `redirect`); `lib/services/orders.ts` (`createPendingOrder` transaccional, `getOrderById`); formulario integrado en `/producto/[slug]` (CTA habilitado) y stub `/checkout/[orderId]`; Vitest (18 tests) + lint + build OK; smoke test real: submit → 303 → checkout 200 → persistencia verificada (pedido de prueba luego eliminado). Se regeneró el cliente Prisma para incluir `Order`/`FormSubmission`. Nota: `tests/setup.ts` mockea `server-only` para Vitest.
- **Bootstrap** de la app en `app-random-dreams/`: Next.js 15.5.22 + React 19 + TS + Tailwind 4 + ESLint; overrides de `sharp` y `postcss` (0 vulns en producción); `.env.example`.
- **Feature 001 · Catálogo y selección** implementada: Prisma 7 + PostgreSQL (Supabase) con migración `init_catalogo` y seed idempotente de los 6 productos (`prisma/seed.ts` + `seed-data.ts`); capa `lib/db/prisma.ts` + `lib/services/products.ts`; design tokens del módulo 0.3 en `globals.css` (@theme) + Navbar/Footer; home = catálogo, `/catalogo` (redirect), ficha `/producto/[slug]` con 404; Vitest (7 tests). Nota: Prisma 7 exige driver adapter (`@prisma/adapter-pg`) y el tipo de modelo se llama `ProductModel`.
- Completar módulo 0.3 (UX/UI) y 0.4 (modelado de datos) en `app-random-dreams/docs/`; prototipo navegable en `app-random-dreams/prototype/`; `docs/contrato-ia.md` (contrato de IA v1.0).

## 2026-07-31

- Reescribir `docs/ARQUITECTURA.md` para el proyecto **Random Dreams** (generación IA de contenido personalizado): pipeline Inngest (Gemini + Hugging Face), contrato de IA, modelo de datos (User, Product, Order, FormSubmission, GeneratedResult, StorageAsset, GenerationLog), routing y layout.
- Reescribir `docs/CONVENCIONES.md` para **Random Dreams**: convenciones de proveedores de IA, pipeline Inngest, checkout sin pasarela, storage y testing con mocks de IA.
- Regenerar `ia-tools/plan-ejecucion.md` con el plan y stack de **Random Dreams**: Next.js 15 full-stack, Prisma/PostgreSQL, Gemini (texto) + Hugging Face (imágenes), Inngest, Supabase Storage (V1), Auth.js (V1), sin pasarela de pago.

### Historial previo

- Crear `docs/ARQUITECTURA.md` con la arquitectura del e-commerce de artículos de fantasía/coleccionables (reemplazado por Random Dreams).
- Crear `docs/CONVENCIONES.md` con las convenciones del e-commerce de fantasía (reemplazado por Random Dreams).
- Crear `ia-tools/plan-ejecucion.md` con el plan del e-commerce de fantasía (reemplazado por Random Dreams).
